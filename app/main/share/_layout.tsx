import { Stack } from 'expo-router';

// Layout for the share section.  This returns a default Stack; you can add
// screen options or nested stacks here if needed.
export default function ShareLayout() {
  return <Stack />;
}