import { Stack } from 'expo-router';

// Layout for authentication screens.  At the moment we simply return a
// default Stack without any custom options.  You can customise headers or
// screen animations here if required.
export default function AuthLayout() {
  return <Stack />;
}